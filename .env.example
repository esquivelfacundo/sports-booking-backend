# Environment Variables Template for Sports Booking Backend
# Copy this file to .env for local development
# For production (Railway/Render), set these variables in your dashboard

# ===========================================
# SERVER CONFIGURATION
# ===========================================
NODE_ENV=development
PORT=8001

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Option 1: Use DATABASE_URL (recommended for Railway, Render, Supabase, Neon)
# Railway will auto-inject this variable when you add a PostgreSQL database
DATABASE_URL=postgresql://username:password@host:port/database_name

# Option 2: Separate config (for local PostgreSQL)
# DB_NAME=sports_booking_db
# DB_USER=postgres
# DB_PASSWORD=password
# DB_HOST=localhost
# DB_PORT=5432

# ===========================================
# JWT CONFIGURATION
# ===========================================
JWT_SECRET=your-super-secret-jwt-key-here-min-32-chars
JWT_REFRESH_SECRET=your-super-secret-refresh-key-here-min-32-chars
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# ===========================================
# ADMIN CONFIGURATION
# ===========================================
ADMIN_INIT_KEY=your-secret-admin-key-2024

# ===========================================
# CORS CONFIGURATION
# ===========================================
# Local development
FRONTEND_URL=http://localhost:4555
# Production (Vercel)
FRONTEND_PROD_URL=https://www.miscanchas.com

# ===========================================
# RATE LIMITING
# ===========================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# OPTIONAL SERVICES
# ===========================================

# Redis (Optional - for caching/sessions)
# REDIS_URL=redis://localhost:6379

# Email (Optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# MercadoPago (Optional - for payments)
# MERCADOPAGO_ACCESS_TOKEN=your-mercadopago-access-token
# MERCADOPAGO_PUBLIC_KEY=your-mercadopago-public-key

# Cloudinary (Optional - for image uploads)
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret

# ===========================================
# RAILWAY DEPLOYMENT NOTES
# ===========================================
# 1. Create a new project in Railway
# 2. Add a PostgreSQL database (Railway will auto-inject DATABASE_URL)
# 3. Deploy from GitHub repo
# 4. Set the following environment variables in Railway dashboard:
#    - NODE_ENV=production
#    - JWT_SECRET (generate a secure random string)
#    - JWT_REFRESH_SECRET (generate a secure random string)
#    - ADMIN_INIT_KEY (your admin initialization key)
#    - FRONTEND_URL (your Vercel frontend URL)
#    - FRONTEND_PROD_URL (your production domain)
# 5. Railway will auto-detect the start command from package.json
